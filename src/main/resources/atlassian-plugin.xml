<atlassian-plugin key="${atlassian.plugin.key}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}"/>
        <param name="plugin-icon">images/pluginIcon.png</param>
        <param name="plugin-logo">images/pluginLogo.png</param>
    </plugin-info>

    <repository-hook name="Url Commit Hook" key="url-commit-hook"
                     class="com.rg.bitbucket.plugin.commit.url.hook.UrlCommitHook">
        <description>A simple pre commit hook that shows the commit Url.</description>
        <icon>images/pluginIcon.png</icon>
    </repository-hook>

</atlassian-plugin>